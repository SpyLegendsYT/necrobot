General Layout
Input: List of 24 discord.Member called member_list
Outpust: Embed

#bloodbath
dead_tributes = list()
for member in member_list:
    die? = random(True, False, False, False)
    if die?:
        print(random.bloodbath_fatal_message)
        member_list.remove(member)
        dead_tributes.append(member)
    else
        print(random.bloodbath_nonfatal_message)

#regular days
days = 0
while member_list > 1:
    if days > 5 and days < 7:
        for x in member_list:
            die? = random(True, False, False, False)
            if die?:
                print(random.feast_fatal_message)
                member_list.remove(member)
                dead_tributes.append(member)
            else
                print(random.feast_nonfatal_message)

    for x in member_list:
        die? = random(True, False, False, False)
        if die?:
            print(random.day_fatal_message)
            member_list.remove(member)
            dead_tributes.append(member)
        else
            print(random.day_nonfatal_message)

    print(dead_tributes)
    clear(dead_tributes)

    for x in member_list:
        die? = random(True, False, False, False)
        if die?:
            print(random.night_fatal_message)
            member_list.remove(member)
            dead_tributes.append(member)
        else
            print(random.night_nonfatal_message)

    day += 1

print(member_list[0] is the winner)

