Input: list of tributes: trib_list
Outpoust: updated list of tributes: trib_list

idle_list = trib_list
dead_list = list()
event_list = events[time]
while idle_list > 0:
    event = random.random([event in event_list if event["tributes"] <= len(idle_list)])
    event_list.remove(event)
    tributes = random.sample(idle_list, event["tributes"])
    idle_list.remove(tributes)
    try:
        for x in event["killed"]:
            tribute = tributes[x-1]
            trib_list.remove(tribute)
            dead_list.append(tribute)

    print(event["string"].format(*tributes))
    clear(tributes)